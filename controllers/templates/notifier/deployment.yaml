---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: notifier
  template:
    spec:
      containers:
        - name: clair
          env:
            - name: CLAIR_MODE
              value: notifier
