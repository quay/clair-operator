---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: indexer
  template:
    spec:
      containers:
        - name: clair
          env:
            - name: CLAIR_MODE
              value: indexer
